#include <iostream>
#include <vector>
#include <algorithm>
#include <queue>
#include "BinaryHeap.h"
#include "constant.h"
using namespace std;

class Dijkstra
{
public:
	static bool GetShortestWay(int **graph, int diem_dau, int diem_cuoi, Data & the_shortest_way);
	//static int dijkstra(int a, int b, vector<Edge>& ways, const vector<Edge> &E, const vector<iter> &s);
};



